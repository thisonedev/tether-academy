---
title: Classify an image
sourceExample: "packages/sdk/examples/classification/classify-image.ts"
platforms: ['node']
tests:
  - id: loads-classification
    description: 'Loads a model with modelType ggml-classification'
    pattern: "loadModel\\s*\\(\\s*\\{[^}]*modelType\\s*:\\s*[`\"']ggml-classification[`\"']"
  - id: reads-image
    description: 'Reads an image with fs.readFileSync'
    pattern: "fs\\.readFileSync"
  - id: calls-classify
    description: 'Calls classify({ modelId, image })'
    pattern: "classify\\s*\\(\\s*\\{[^}]*modelId[^}]*image"
  - id: iterates-results
    description: 'Loops over results and logs label and confidence'
    pattern: "for\\s*\\(\\s*const\\s+(?:\\{|\\w+\\s+of\\s+)results|results\\.forEach|for\\s*\\(\\s*const\\s*\\{[^}]*\\}\\s*of\\s+results"
hints:
  - "loadModel({ modelType: 'ggml-classification' }) with no modelSrc. The model is bundled inside @qvac/classification-ggml."
  - "Read the image as a Buffer with fs.readFileSync(path). classify({ modelId, image }) returns an array of { label, confidence } sorted by score."
expectedOutput:
  - "Classification results:"
  - "  food: 87.4%"
  - "  report: 11.2%"
  - "  other: 1.4%"
questions:
  - id: why-no-modelsrc
    text: "Why does `loadModel` need no `modelSrc` for the classification model?"
    answers:
      - text: "The model is bundled inside the `@qvac/classification-ggml` addon; other models are the ones downloaded from the registry."
        correct: true
      - text: "The classification model doesn't need to be loaded at all before use."
        correct: false
        feedback: "Re-read whether `loadModel` is still called, just without `modelSrc`."
      - text: "`modelSrc` is optional for every model; classification isn't a special case."
        correct: false
        feedback: "Check whether other lessons' `loadModel` calls also omit `modelSrc`, or whether this is specific to classification."
  - id: why-model-intentionally-small
    text: "What lets the bundled MobileNetV3-Small classifier run in tens of milliseconds on a single CPU core?"
    answers:
      - text: "It's a small model on purpose, sized for fast inference rather than maximum accuracy."
        correct: true
      - text: "Because larger classifiers aren't compatible with the ggml-classification `modelType`."
        correct: false
        feedback: "Think about whether `modelType` is described as size-limited, or whether size is a design choice."
      - text: "Because it only needs to support three output classes."
        correct: false
        feedback: "Check whether the model's size is tied to output class count, or to inference speed."
---

We're going to add one more image-based capability, separate from generation.

Image classification takes an image and returns one or more category labels with confidence scores. The SDK includes a small bundled MobileNetV3-Small model that produces three classes: `food`, `report`, `other`. It's useful for routing (which model to invoke), filtering (does this image contain X?), and tagging at scale.

The flow is similar to the other capabilities: load the model, call the function, unload the model. The classification model is bundled in the addon, so `loadModel` takes no `modelSrc`:

```ts
const modelId = await loadModel({ modelType: "ggml-classification" });
```

The result is sorted by score descending, so `results[0]` is the top guess. The SDK returns a `confidence` score between 0 and 1, so multiplying by 100 will get you a percentage:

```ts
const image = fs.readFileSync("./examples/image/basic_test.jpg");
const results = await classify({ modelId, image });

for (const { label, confidence } of results) {
  console.log(`  ${label}: ${(confidence * 100).toFixed(1)}%`);
}
```

Cleanup means freeing the model, same as any other capability:

```ts
await unloadModel({ modelId });
```

The notable difference from other lessons: `loadModel` takes no `modelSrc`. The classification model is bundled inside the `@qvac/classification-ggml` addon; the registry download step other lessons use doesn't apply here. The `modelType: "ggml-classification"` flag tells the SDK which addon to route through.

> Note: the bundled MobileNetV3-Small model is small on purpose. It runs in tens of milliseconds on a single CPU core. If you need a bigger or domain-specific classifier, you'd bring your own GGUF and add it to a custom addon.