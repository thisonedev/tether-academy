---
title: "Connect a delegated-inference consumer"
sourceExample: "packages/sdk/examples/delegated-inference/consumer.ts"
platforms: ['node']
argv:
  - name: providerPublicKey
    from: state:lastProviderPublicKey
    label: "Provider public key"
tests:
  - id: delegate-public-key
    description: 'Passes providerPublicKey under a `delegate` block to loadModel'
    pattern: "delegate\\s*:\\s*\\{[^}]*providerPublicKey"
  - id: timeout-set
    description: 'Sets a generous timeout on the delegate'
    pattern: "delegate\\s*:\\s*\\{[^}]*timeout"
  - id: fallback-to-local
    description: 'Sets fallbackToLocal on the delegate'
    pattern: "fallbackToLocal"
  - id: runs-completion
    description: 'Calls completion() against the delegated modelId'
    pattern: "completion\\s*\\(\\s*\\{[^}]*modelId"
  - id: streams-tokens
    description: 'Iterates response.tokenStream to stdout'
    pattern: "for\\s+await.*tokenStream"
hints:
  - "loadModel({ modelSrc, delegate: { providerPublicKey, timeout: 60_000, fallbackToLocal: true } }) registers a model that runs on a remote peer."
  - "The first call on a cold DHT takes 15 to 45 seconds for the consumer to bootstrap hyperdht and look up the provider's key. The timeout gives that headroom. Subsequent connections in the same process are sub-second."
  - "fallbackToLocal: true means the consumer will run the model locally if the delegate is unreachable. Useful while the provider is restarting or offline."
expectedOutput:
  - "▸ Testing delegated inference"
  - "▸ Provider: 7f3a2b1c..."
  - "▸ Delegated model registered: model-7f2a"
  - "Hello there!"
  - "▸ Stats: { tokensPerSecond: 47.31 }"
questions:
  - id: why-generous-timeout
    text: "What is a key consideration behind setting `timeout: 60_000` for the delegate block, instead of a shorter default?"
    answers:
      - text: "The first call on a cold DHT can take 15 to 45 seconds to bootstrap and find the provider, so the timeout needs that headroom; later calls in the same process are much faster."
        correct: true
      - text: "The timeout only applies when `fallbackToLocal` is false."
        correct: false
        feedback: "Re-read whether timeout is tied to `fallbackToLocal` at all."
      - text: "60 seconds is how long a single token takes to stream back from the provider."
        correct: false
        feedback: "Check what the timeout covers: DHT bootstrap, or per-token streaming."
  - id: what-fallbacktolocal-does
    text: "What happens if the provider is unreachable when `fallbackToLocal: true` is set?"
    answers:
      - text: "The consumer retries the same provider until the timeout expires, then gives up."
        correct: false
        feedback: "Think about whether a retry loop is described, or a switch to local execution."
      - text: "`completion()` throws immediately without attempting a fallback."
        correct: false
        feedback: "Check what `fallbackToLocal` does when the delegate can't be reached."
      - text: "The consumer runs the model on its own hardware instead; the call still succeeds, with a different latency profile."
        correct: true
  - id: what-modelsrc-mismatch-does
    text: "Does `fallbackToLocal` catch a `modelSrc` mismatch the same way it catches an unreachable provider?"
    answers:
      - text: "Yes: the provider automatically loads the requested model on demand instead of rejecting."
        correct: false
        feedback: "Check whether a mismatch triggers automatic loading, or a rejection."
      - text: "No: the provider's router rejects the request with a model-mismatch error instead."
        correct: true
      - text: "Yes: `fallbackToLocal` silently handles the mismatch by running locally, same as an unreachable provider."
        correct: false
        feedback: "Think about whether `fallbackToLocal` is triggered by a mismatch, or only by an unreachable provider."
---

The previous lesson booted a provider. This one consumes it.

The runner captures the provider's public key from the previous run's output and passes it to this snippet as `process.argv[2]`. You don't have to copy anything. If you want to connect to a different provider (a peer you don't own, a test instance on another machine), type the key into the **Provider public key** field above the editor. That value takes precedence over the captured one and persists across runs, so you only set it once.

The consumer side is the same `loadModel()` / `completion()` interface as every other lesson. The only new field is `delegate`, which routes the inference to a peer instead of running it locally.

The `loadModel()` call takes the same `modelSrc` parameter as every other lesson. It also takes `delegate`, which carries the routing fields (`providerPublicKey`, `timeout`, `fallbackToLocal`):

```ts
const modelId = await loadModel({
  modelSrc: LLAMA_3_2_1B_INST_Q4_0,
  delegate: {
    providerPublicKey,
    timeout: 60_000,
    fallbackToLocal: true,
  },
});
```

The `completion()` call uses the same `modelId`, `history`, and `stream: true` signature as the text-generation lessons. The `modelId` here is the delegate's handle, so the call routes through the peer over the DHT:

```ts
const response = completion({
  modelId,
  history: [{ role: "user", content: "Hello!" }],
  stream: true,
});
```

The loop iterates `response.tokenStream` and writes each token to stdout. The `response.stats` Promise resolves once the stream ends, so logging it after the loop gives the per-call metrics:

```ts
for await (const token of response.tokenStream) {
  process.stdout.write(token);
}
console.log("\n▸ Stats:", await response.stats);
```

The `delegate` block takes the provider's public key and a generous timeout. The first call on a cold DHT needs 15 to 45 seconds: bootstrapping hyperdht, looking up the provider's key, opening the connection. The SDK gives that headroom via `timeout: 60_000`. Once the DHT is warm, subsequent connections in the same process are sub-second.

`fallbackToLocal: true` is the safety net. If the provider is unreachable (restarting, offline, behind a firewall the consumer can't traverse), the consumer falls back to running the model on its own hardware. The call still succeeds, though with a different latency profile.

> Note: the consumer's `modelSrc` has to match the provider's loaded model. If the provider has Llama 3.2 1B loaded, the consumer has to ask for the same constant, or the provider's router will reject the request with a model-mismatch error.