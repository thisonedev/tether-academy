---
title: "Track diffusion progress"
sourceExample: "packages/sdk/examples/diffusion-flux2-klein.ts"
platforms: ['node']
tests:
  - id: reads-progress-stream
    description: Iterates result.progressStream
    pattern: "progressStream"
  - id: prints-step
    description: Logs step / totalSteps
    pattern: "step\\s*/\\s*totalSteps|console\\.log\\s*\\([^)]*progress\\.step"
hints:
  - "result.progressStream is an AsyncIterable. Each item is { step, totalSteps }, the current step and the total."
  - "Print every step for small runs, sample one-in-N for long ones, keeps the console readable."
  - "The model continues to refine after each tick; await result.outputs still runs the same inference end-to-end."
expectedOutput:
  - "1/20"
  - "..."
  - "20/20"
  - "Generated 1 image"
questions:
  - id: why-progress-and-outputs-dont-block
    text: "What's the relationship between `result.progressStream` and `result.outputs` during one `diffusion()` call?"
    answers:
      - text: "outputs only resolves after `progressStream` is fully drained, so the two run one after the other with no overlap."
        correct: false
        feedback: "Re-read what the concept paragraph says about whether these two block each other."
      - text: "The progress stream and the output promise don't block each other; draining one doesn't stop the underlying inference."
        correct: true
      - text: "`progressStream` and outputs come from two completely separate `diffusion()` calls."
        correct: false
        feedback: "Check whether the code above calls `diffusion()` once or twice."
  - id: why-guard-progressstream
    text: "What has to be true about `result.progressStream` before the code can safely iterate it?"
    answers:
      - text: "The check prevents `diffusion()` from running twice."
        correct: false
        feedback: "Check what this specific guard is defensive against."
      - text: "`progressStream` is undefined until the first denoising step completes."
        correct: false
        feedback: "Think about whether the check is about timing within a single call, or about which models expose the field at all."
      - text: "`progressStream` is optional in the type system, since older model versions didn't expose it."
        correct: true
---

Now that we're running diffusion calls that take real time, let's see how to show progress.

`diffusion()` runs for tens of seconds. Without feedback, the user thinks it crashed. The same `result` we used in the previous lesson has a second property: `progressStream`.

`progressStream` ticks once per denoising step. Each item is `{ step, totalSteps }`. The total tells us the upper bound. The step tells us where the model is right now.

We loop the stream in parallel with the final `await result.outputs`, since they don't block each other.

The per-step progress stream carries `{ step, totalSteps }` once per denoising tick, same `for await` pattern as the `tokenStream` drain in the text lessons like so:

```ts
if (result.progressStream) {
  for await (const progress of result.progressStream) {
    console.log(`${progress.step}/${progress.totalSteps}`);
  }
}
```

The progress stream and the output promise don't block each other, so you can run them in parallel:

```ts
const outputs = await result.outputs;
const first = outputs[0];
if (first) fs.writeFileSync("output/image-generation/skyline.png", first);
console.log(`Generated ${outputs.length} image`);
```

We wire this stream into our app's progress bar and the user sees "5/20, 6/20, ..." tick up while the model works.

> Note: `progressStream` is optional in the type system. The check `if (result.progressStream)` is defensive; older model versions didn't expose it. New models all do.