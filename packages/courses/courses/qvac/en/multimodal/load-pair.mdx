---
title: Load a multimodal model and projection
sourceExample: "packages/sdk/examples/llamacpp-multimodal.ts"
platforms: ['node']
tests:
  - id: passes-projection
    description: Passes the projection model via modelConfig.projectionModelSrc
    pattern: "projectionModelSrc\\s*:"
  - id: stores-id
    description: Stores the loaded model in a multimodalId variable
    pattern: "multimodalId\\s*=\\s*await\\s+loadModel"
hints:
  - "Multimodal needs two GGUF files: the LLM (like SMOLVLM2_500M_MULTIMODAL_Q8_0) and its matching mmproj projector."
  - "Both come from @qvac/sdk as named constants. Pass the projector via modelConfig.projectionModelSrc."
  - "The pair has to match. SmolVLM2 pairs with mmproj-smolvlm2. You can't mix an LLM constant with a different model's projector."
expectedOutput:
  - "multimodalId: model-7f2a"
questions:
  - id: why-two-files
    text: "Why does loading a multimodal model need two files instead of one?"
    answers:
      - text: "The language model handles text, but a separate projection model is needed to turn an image into the same vector space the language model operates in."
        correct: true
      - text: "One file holds the model weights and the other is a config file with hyperparameters."
        correct: false
        feedback: "Check what the concept paragraph says the second file actually does with an image."
      - text: "The second file is a backup in case the first model fails to load."
        correct: false
        feedback: "Re-read what projectionModelSrc actually does, not just that it's present."
  - id: mismatched-projector
    text: "What happens if SMOLVLM2_500M_MULTIMODAL_Q8_0 is paired with the wrong mmproj projector?"
    answers:
      - text: "It produces nonsense output, since the two constants have to come from the same model family."
        correct: true
      - text: "loadModel throws immediately and refuses to load the mismatched pair."
        correct: false
        feedback: "Check what the Note says actually happens with a mismatched pair, not whether it's blocked."
      - text: "The SDK automatically substitutes the matching projector."
        correct: false
        feedback: "Think about whether the SDK validates or corrects this pairing at all."
---

We're starting a new chapter on multimodal models, and we're going to load a model that takes images alongside text.

Text-only LLMs load from one file. Multimodal LLMs need two: the language model itself, and a small "projection" model that turns an image into the same kind of vector space the language model operates in.

`loadModel()` accepts both at once. The main file goes in `modelSrc`. The projector goes in `modelConfig.projectionModelSrc`.

Multimodal = LLM in `modelSrc` + mmproj projector in `projectionModelSrc`. Both files must be from the same model family. You would load them like so:

```ts
const multimodalId = await loadModel({
  modelSrc: SMOLVLM2_500M_MULTIMODAL_Q8_0,
  modelConfig: {
    projectionModelSrc: MMPROJ_SMOLVLM2_500M_MULTIMODAL_Q8_0,
  },
});
console.log("multimodalId:", multimodalId);
```

`multimodalId` looks the same as every other `modelId`. The difference is hidden in how `completion()` will treat `attachments` on history messages, which we'll see in the next two lessons.

> Note: the two constants have to come from the same model family. `SMOLVLM2` pairs with `MMPROJ_SMOLVLM2`. Mixing an LLM constant with the wrong projector (or a non-multimodal LLM with a projector) produces nonsense output.