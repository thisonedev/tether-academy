---
title: Embed a single string
sourceExample: "packages/sdk/examples/embed-p2p.ts"
platforms: ['node']
tests:
  - id: passes-modelid
    description: Calls embed() with modelId from the previous lesson
    pattern: "embed\\s*\\(\\s*\\{[^}]*modelId"
  - id: passes-text
    description: Passes a `text` option
    pattern: "embed\\s*\\(\\s*\\{[^}]*text\\s*:"
  - id: destructures-embedding
    description: Destructures `embedding` from the result
    pattern: "embedding\\s*:|\\{\\s*embedding"
hints:
  - "embed is a top-level export from @qvac/sdk. It takes { modelId, text }."
  - "It returns { embedding } where embedding is a number[]. For GTE_LARGE_FP16 it is 1024 numbers long."
  - "You probably don't want to print every number. Print the length and the first 10 values to check it's real."
expectedOutput:
  - "Input: 'Hello, world!'"
  - "Embedding dimensions: 1024"
  - "First 10 values: [ 0.0123 ... ]"
questions:
  - id: embed-return-type
    text: "What does `embed` resolve to for a single string?"
    answers:
      - text: "An object with an embedding property holding a 1024-number array."
        correct: true
      - text: "A single float scoring how positive or negative the text is."
        correct: false
        feedback: "Check what type the concept paragraph assigns to embedding, not a single score."
      - text: "The original string and a computed hash."
        correct: false
        feedback: "Look at what the destructure in the code example pulls out of the result."
  - id: embedding-consistency-across-inputs
    text: "Given a short sentence and a full paragraph embedded with the same model, what stays the same between the two results?"
    answers:
      - text: "Longer input produces a longer array."
        correct: false
        feedback: "Re-read what stays fixed regardless of input length."
      - text: "The exact numeric values, since `embed()` ignores the input text's content."
        correct: false
        feedback: "Think about whether two different sentences could really produce identical numbers."
      - text: "The length: both come back as a 1024-number array, though the values inside differ."
        correct: true
---

Now that we have a model in memory, let's hand it a string and see what comes back.

`embed({ modelId, text: "..." })` resolves to `{ embedding: number[] }`. For `GTE_LARGE_FP16` the array has 1024 numbers, one per feature the model learned during training. Each number is a small float, mostly between -1 and 1.

We don't need to understand what each number means yet. We're going to compare vectors in the next lessons. For now, the part to remember is that every input produces a 1024-number array on `GTE_LARGE_FP16`.

The `embed()` call for one text takes a modelId and a single `text` string. For `GTE_LARGE_FP16` the return is a 1024-number array. You would call it like so:

```ts
const { embedding } = await embed({
  modelId,
  text: "Hello, world!",
});
console.log("Input:", "Hello, world!");
console.log("Embedding dimensions:", embedding.length);
console.log("First 10 values:", embedding.slice(0, 10));
```

`embedding` is a `number[]`. Use `.length` for the dimension and `.slice(0, 10)` to peek at the first few values without spamming the console.

> Note: the SDK always returns the same `number[]` length regardless of how long the input text is. Short sentences and paragraphs both produce a 1024-number vector for `GTE_LARGE_FP16`. The numbers are different, but the length is identical.