---
title: "Read the stop reason from a completion"
sourceExample: "packages/sdk/examples/completion-stop-reason.ts"
platforms: ['node']
tests:
  - id: awaits-final
    description: Awaits result.final to read the aggregate
    pattern: "await\\s+result\\.final"
  - id: reads-stopreason
    description: Reads final.stopReason
    pattern: "final\\.stopReason"
  - id: branches
    description: 'Branches on the stopReason value'
    pattern: "stopReason\\s*===\\s*\"(length|cancelled)\""
  - id: sets-budget
    description: Sets a token budget via generationParams.predict
    pattern: "predict\\s*:"
hints:
  - "result.final is a Promise<CompletionFinal>. Await it to get the aggregate after the stream ends."
  - "stopReason is undefined on a natural end-of-sequence, 'length' when the token budget was hit, 'cancelled' if the request was cancelled."
  - "Set a tight token budget with generationParams: { predict: 10 } so the lesson's simulated output shows the truncated path."
  - "A small prompt like 'Say hi in one word.' with predict: 10 lets you see the truncated path reliably."
expectedOutput:
  - "▸ Completion started (predict: 10)"
  - "▸ Stream ended (10 tokens emitted)"
  - "▸ truncated: model hit the token budget"
questions:
  - id: what-length-stopreason-means
    text: "Which of the following is NOT a possible value of `stopReason`?"
    answers:
      - text: "`\"timeout\"`"
        correct: true
      - text: "`\"cancelled\"`"
        correct: false
        feedback: "Compare this to what \"cancelled\" means above; it's one of the three real values."
      - text: "`undefined`"
        correct: false
        feedback: "Check what `undefined` is paired with above: a truncated output, or a natural end of sequence. It's one of the three real values."
  - id: why-drain-for-final
    text: "What has to be true about `result.tokenStream` for `result.final` to resolve?"
    answers:
      - text: "Nothing; `final` resolves independently of whether the stream was ever drained."
        correct: false
        feedback: "Re-read what happens to `result.final` if the stream is never drained."
      - text: "It has to be fully drained; an undrained stream backs up and `result.final` never resolves."
        correct: true
      - text: "A partial drain, just the first few tokens, is enough; the rest never needs draining."
        correct: false
        feedback: "Think about how much of the stream has to be read before `final` resolves: some of it, or all of it."
  - id: why-tight-token-budget
    text: "What is the primary purpose of setting a tight `generationParams` token budget on a short prompt in this demo?"
    answers:
      - text: "To force the truncated path (`stopReason` \"length\") reliably, for demonstration."
        correct: true
      - text: "A tight token budget is required for every `completion()` call regardless of purpose."
        correct: false
        feedback: "Check what's said above about token budgets in production versus in this demo."
      - text: "The token budget setting shortens the prompt itself before it's sent."
        correct: false
        feedback: "Think about whether this setting controls what's generated, or what's sent in."
---

Now that we know how to iterate the event stream, let's look at what we get when the stream ends.

Every `CompletionFinal` carries a `stopReason` that explains how the model stopped generating:

- `undefined`, natural end of sequence (EOS). The model finished on its own. This is the common case.
- `"length"`, the `predict` token budget was exhausted. Output is truncated, the model did not reach a natural stopping point.
- `"cancelled"`, the request was cancelled via `cancel({ requestId })`.

Setting `predict: 10` forces the truncated path. You would call it in the following way:

```ts
const result = completion({
  modelId,
  history: [{ role: "user", content: "Say hi in one word." }],
  captureThinking: true,
  generationParams: { predict: 10 },
  stream: true,
});
```

The drain is necessary even when we only care about `final`. Note that without it, the stream backs up and `result.final` never resolves. You would write the drain like so:

```ts
for await (const token of result.tokenStream) process.stdout.write(token);
```

After the drain, we read the aggregate. `result.final` is the canonical surface for it. Note that the `await` is what fetches the aggregated `contentText`, `thinkingText`, `toolCalls`, `stats`, and `stopReason`:

```ts
const final = await result.final;
```

Branching on `stopReason === "length"` is how we surface the truncation. Note that the only "length" value here is the budget-truncation path; `"cancelled"` is a separate branch. Consider the following example:

```ts
if (final.stopReason === "length") {
  console.log("▸ truncated: model hit the token budget");
}
```

> Note: a tight `predict` budget on a short prompt is the easiest way to see the truncation path. In production you usually want a generous budget and rely on EOS, but reading `stopReason` is how you tell the two apart after the fact.