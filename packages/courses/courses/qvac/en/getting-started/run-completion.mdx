---
title: Run a completion
sourceExample: "packages/sdk/examples/quickstart.ts"
platforms: ['node']
tests:
  - id: calls-completion
    description: Calls completion() with modelId and history
    pattern: "completion\\s*\\(\\s*\\{[^}]*modelId[^}]*history"
  - id: history-shape
    description: 'History is an array of {role, content} messages'
    pattern: "\\{\\s*role\\s*:\\s*\"user\""
  - id: enables-stream
    description: "Enables streaming via stream: true"
    pattern: "stream\\s*:\\s*true"
  - id: iterates-tokens
    description: Iterates result.tokenStream with for-await-of
    pattern: "for\\s+await.*tokenStream"
hints:
  - "completion() takes an options object. The two required keys are modelId (from the previous lesson) and history (an array of messages)."
  - "Each history message is { role: 'user' | 'assistant' | 'system', content: '...' }. Start with one user message."
  - "Set stream: true in the options so you can iterate tokens. The returned result.tokenStream is an async iterable of strings."
  - "Use for await (const token of result.tokenStream) and write each token to stdout."
expectedOutput:
  - "▸ Completion started"
  - "Quantum computing uses qubits that can exist in multiple states at once through superposition."
questions:
  - id: what-stream-true-returns
    text: "What does `completion()` return when you pass `stream: true`?"
    answers:
      - text: "A result that blocks until the entire response is generated, same as without stream."
        correct: false
        feedback: "Think about what changes in how you receive the response when streaming is on."
      - text: "A result whose tokenStream you can iterate to see tokens come in one at a time, instead of waiting for the whole response."
        correct: true
      - text: "A history array that must include a system prompt before streaming works."
        correct: false
        feedback: "Check how many messages the history example above actually has."
  - id: what-is-in-history
    text: "What's inside each entry of the `history` array?"
    answers:
      - text: "A single string containing the entire conversation so far."
        correct: false
        feedback: "Look at the shape of the history array in the code example."
      - text: "{ role, content } pairs, like a chat log."
        correct: true
      - text: "The modelId and a timestamp for each turn."
        correct: false
        feedback: "Check what fields are actually shown on each history entry in the example."
  - id: full-text-after-stream
    text: "How can you get the completion's full text after the stream ends, instead of assembling it token by token?"
    answers:
      - text: "await result.text: it's available as a single string once the stream finishes."
        correct: true
      - text: "You have to concatenate every token yourself; there's no other way."
        correct: false
        feedback: "Re-read the Note about what's available once the stream ends."
      - text: "Call completion() again without stream: true."
        correct: false
        feedback: "Think about whether this needs a new call, or if it's already on the same result object."
---

Now that we have a `modelId`, it's time to ask the model to say something.

`completion()` takes a `modelId` and a `history` (an array of messages), and gives us back a result we can either await fully or stream token by token. We pass `stream: true` so we can watch the tokens arrive.

The `history` array is a list of `{ role, content }` messages, like a chat log. We're starting with one user message, but later lessons will add assistant and system turns.

A `history` array with one user message would look like this:

```ts
const history = [
  { role: "user", content: "Explain quantum computing in one sentence." },
];
```

Now we wrap that history in a `completion()` call like so:

```ts
const result = completion({ modelId, history, stream: true });
```

Finally, we drain the token stream token-by-token using `process.stdout.write`:

```ts
for await (const token of result.tokenStream) {
  process.stdout.write(token);
}
```

> Note: when the stream finishes, the result is also available as a single string via `await result.text`. We'll use that in later lessons.