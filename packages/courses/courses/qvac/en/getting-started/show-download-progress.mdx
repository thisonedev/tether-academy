---
title: Show download progress
sourceExample: "packages/sdk/examples/quickstart.ts"
platforms: ['node']
tests:
  - id: has-onprogress
    description: loadModel call includes an onProgress option
    pattern: "loadModel\\s*\\(\\s*\\{[^}]*onProgress\\s*:"
  - id: callback-signature
    description: onProgress callback receives a progress object and reads percentage / downloaded / total
    pattern: "onProgress\\s*:\\s*\\(?\\s*p\\)?\\s*=>\\s*\\{[\\s\\S]*p\\.(percentage|downloaded|total)"
  - id: still-loads
    description: Still calls loadModel with modelSrc
    pattern: "loadModel\\s*\\(\\s*\\{[^}]*modelSrc"
  - id: awaits-result
    description: Awaits the result into modelId
    pattern: "modelId\\s*=\\s*await\\s+loadModel"
hints:
  - "onProgress is a key inside the same options object you already pass to loadModel(). Put it next to modelSrc."
  - "The callback runs many times during one download. Each call hands you a progress object with percentage (0 to 100), downloaded bytes, and total bytes."
  - "Write to process.stderr, not process.stdout. Progress is logging; stdout is reserved for the model's actual response in the next lesson."
expectedOutput:
  - "▸ Downloading 7% (50.0/740.0 MB)"
  - "▸ Downloading 38% (281.0/740.0 MB)"
  - "▸ Downloading 71% (525.0/740.0 MB)"
  - "▸ Downloading 100% (740.0/740.0 MB)"
  - "▸ Model loaded: model-7f2a"
  - "modelId: model-7f2a"
questions:
  - id: why-stderr-not-stdout
    text: "Which output stream does the progress callback write its lines to?"
    answers:
      - text: "`process.stdout`, since progress and the model's response can share one stream."
        correct: false
        feedback: "Check what stdout is reserved for, according to the concept paragraph."
      - text: "`process.stderr`, since stdout is reserved for the model's actual response."
        correct: true
      - text: "Neither; progress goes straight to a log file, with no stream involved."
        correct: false
        feedback: "Re-read which `process` stream the callback actually calls `.write` on."
  - id: what-onprogress-receives
    text: "Given the expected output above, what does the second `onProgress` call receive?"
    answers:
      - text: "The percentage, downloaded, and total shown on the second `Downloading` line, with downloaded less than the final line's total."
        correct: true
      - text: "Exactly 50%, since progress always reports in round increments."
        correct: false
        feedback: "Check the actual percentage on the second line in the expected output, not an assumed rounding."
      - text: "The full downloaded file's bytes as a `Buffer`."
        correct: false
        feedback: "Think about whether `onProgress` carries the file's contents or just status numbers."
  - id: why-check-istty
    text: "What determines whether progress output uses `\\r` or `\\n` after each tick?"
    answers:
      - text: "Whether the output is JSON-formatted or plain text."
        correct: false
        feedback: "Check exactly which condition decides between \\r and \\n."
      - text: "`process.stderr.isTTY`: a TTY can overwrite the same line with `\\r`, while piped output needs `\\n` so each tick ends up on its own row."
        correct: true
      - text: "The current download percentage; `\\r` is used below 100% and `\\n` at completion."
        correct: false
        feedback: "Think about what `isTTY` changes: how the terminal renders output, or how far along the download is."
---

In the very first lesson we called `loadModel({ modelSrc: ... })` and waited. From the user's point of view the script sat there with no output while a multi-hundred-megabyte file downloaded. `onProgress` exists to make that wait readable.

`onProgress` is a callback we pass alongside `modelSrc`. The SDK calls it repeatedly while the model downloads. Each call hands us `{ percentage, downloaded, total }`. We print every call, throttle to every few percent, or draw a progress bar.

Let's take a closer look at the pattern from the [QVAC quickstart example](https://github.com/tetherto/qvac/blob/main/packages/sdk/examples/quickstart.ts). The callback goes inside the same `loadModel()` options object that already carries `modelSrc`.

`onProgress` runs many times during a download. The full callback wired into `loadModel()` would look like the following:

```ts
const modelId = await loadModel({
  modelSrc: LLAMA_3_2_1B_INST_Q4_0,
  onProgress: (p) => {
    const mb = (n: number) => (n / 1e6).toFixed(1);
    const line = `▸ Downloading ${p.percentage.toFixed(0)}% (${mb(p.downloaded)}/${mb(p.total)} MB)`;
    process.stderr.write(process.stderr.isTTY ? `\r${line}` : `${line}\n`);
    if (p.percentage >= 100) process.stderr.write("\n");
  },
});
console.log("modelId:", modelId);
```

A few details worth pointing at:

- `process.stderr.write` is the right call here. Stdout is reserved for the model's actual response in the next lesson; progress is logging, and stderr is the standard place for it.
- The `process.stderr.isTTY` flag picks the rendering mode. When the terminal is a TTY, the script writes `\r` so each tick overwrites the same line. When output is piped to a file, it writes `\n` so each tick ends up on its own row in the log.
- The final `process.stderr.write("\n")` after `p.percentage >= 100` makes sure the next log starts on a fresh row.