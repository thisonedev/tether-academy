---
title: "Reindex a RAG workspace after many writes"
sourceExample: "packages/sdk/examples/rag/rag-hyperdb/workspaces.ts"
platforms: ['node']
tests:
  - id: calls-reindex
    description: 'Calls ragReindex with workspace'
    pattern: "ragReindex\\s*\\(\\s*\\{[^}]*workspace[^}]*\\}"
  - id: waits-for-result
    description: 'Awaits the reindex result'
    pattern: "await\\s+ragReindex"
  - id: reads-result
    description: 'Reads result.reindexed and logs the reason when it is false'
    pattern: "result\\.reindexed|details\\?\\.reason"
hints:
  - "ragReindex takes { workspace }. It processes the stored text in the workspace and re-embeds everything."
  - "Use it after many writes (hundreds of deletes and re-ingests) when scores feel off."
  - "The reindex is idempotent, running it twice in a row is safe, the second run is a no-op."
  - "HyperDB's k-means needs at least 4 documents (NUM_CENTROIDS default) to produce centroids. Fewer than that returns { reindexed: false, reason: 'insufficient documents' }."
expectedOutput:
  - "Reindexed: false"
  - "Reason: insufficient documents"
questions:
  - id: why-insufficient-documents
    text: "Why does `ragReindex` return `{ reindexed: false, reason: 'insufficient documents' }` for a small workspace?"
    answers:
      - text: "The workspace's embedding model doesn't support reindexing."
        correct: false
        feedback: "Think about what NUM_CENTROIDS is counting: documents, or models."
      - text: "`ragReindex` only runs on workspaces created with `chunk: true`."
        correct: false
        feedback: "Check what the hints say determines whether a reindex has enough data to run."
      - text: "`HyperDB`'s k-means needs at least 4 documents to produce its centroids, and this workspace falls short."
        correct: true
  - id: why-safe-to-reindex-twice
    text: "What happens if you call `ragReindex` twice in a row?"
    answers:
      - text: "The second call is blocked automatically and returns immediately with an error."
        correct: false
        feedback: "Check whether the concept paragraph describes the second call as blocked, or as a no-op."
      - text: "The operation is idempotent; the second run finds nothing left to rebuild."
        correct: true
      - text: "Each call only reindexes documents added since the previous reindex."
        correct: false
        feedback: "Re-read what `ragReindex` regenerates: everything stored, or just new entries."
---

After enough `ragDeleteEmbeddings` and re-ingest cycles, scores can drift. The index no longer reflects the source text. `ragReindex({ workspace })` processes the stored text for that workspace and regenerates every embedding from scratch.

`ragReindex` runs k-means over the stored embeddings. K-means needs at least `K` samples to produce `K` centroids. HyperDB's `NUM_CENTROIDS` defaults to 4 and is not configurable through the SDK, so any workspace with fewer than 4 documents gets a no-op reindex with `reason: "insufficient documents"`.

The reindex is idempotent: running it twice in a row is safe, the second run finds nothing to do.

The result is `{ reindexed, details }`. `reindexed: true` means anything was rebuilt; `false` carries a `reason` you can log. The call is two lines:

```ts
const result = await ragReindex({ workspace });
console.log("Reindexed:", result.reindexed);
if (!result.reindexed) {
  console.log("Reason:", result.details?.reason ?? "unknown");
}
```

For this 4-recipe demo the reindex deliberately bails. The point of the lesson is to read the result and know what to do with it: in a real workspace with thousands of documents, this branch never fires, you just log the success and proceed with the updated embeddings.

> Note: a reindex on a large workspace takes roughly the same time as the original ingest. Plan to run it during a quiet window or as a one-shot maintenance task.