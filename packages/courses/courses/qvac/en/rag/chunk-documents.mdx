---
title: "Chunk documents for RAG"
sourceExample: "packages/sdk/examples/rag/rag-hyperdb/workspaces.ts"
platforms: ['node']
tests:
  - id: sets-chunk-true
    description: 'Sets chunk: true for long documents'
    pattern: "ragIngest\\s*\\(\\s*\\{[\\s\\S]*?chunk\\s*:\\s*true"
  - id: passes-chunk-opts
    description: 'Passes chunkOpts with size and overlap'
    pattern: "chunkOpts\\s*:\\s*\\{[^}]*chunkSize"
  - id: logs-chunk-count
    description: Logs how many chunks came back
    pattern: "console\\.log\\s*\\([^)]*processed\\.length|console\\.log\\s*\\([^)]*chunks"
hints:
  - "Set chunk: true when your documents are longer than a few hundred words. The SDK splits them before embedding."
  - "chunkOpts.chunkSize is the target size per chunk in tokens. chunkOverlap is how much neighboring chunks share, keeps context that crosses a boundary."
  - "result.processed.length is the chunk count, not the document count. One long doc becomes many chunks."
expectedOutput:
  - "Created 12 chunks from 3 documents"
questions:
  - id: why-long-docs-need-chunking
    text: "Why can't a 5000-word article be embedded as a single input?"
    answers:
      - text: "The embedding model has a fixed context, far shorter than the article, so the text has to be split first."
        correct: true
      - text: "ragIngest refuses any document longer than 200 words."
        correct: false
        feedback: "Check what chunkSize actually configures, compared to what limits a single embed call."
      - text: "Long documents produce a longer embedding array than short ones do."
        correct: false
        feedback: "Recall what stayed fixed regardless of input length, back in the embeddings chapter."
  - id: what-chunkoverlap-does
    text: "What does chunkOverlap do, and why does it matter?"
    answers:
      - text: "It keeps a few shared tokens between neighboring chunks so a sentence that straddles a boundary isn't lost."
        correct: true
      - text: "It controls the total number of chunks created, regardless of document length."
        correct: false
        feedback: "Check which option targets chunk size versus which one shares context at the boundary."
      - text: "It merges chunks from different documents into a single entry."
        correct: false
        feedback: "Re-read what stays scoped per document once chunking runs."
  - id: what-chunked-result-represents
    text: "After chunking, what does a search result represent that it didn't before?"
    answers:
      - text: "The specific passage that matched, rather than the whole document."
        correct: true
      - text: "The full original document, exactly as it did before chunking was enabled."
        correct: false
        feedback: "Check what processed contains after a chunked ingest, compared to a whole-document one."
      - text: "A summary generated from the matching chunk."
        correct: false
        feedback: "Think about whether anything gets generated here, or whether the chunk is just returned as stored."
---

Now that we know how to ingest and search, let's handle longer documents.

A 1024-token embedding model can't take a 5000-word article as a single input. `chunk: true` tells `ragIngest` to split documents before embedding. The search results we saw before were whole documents. Chunked search returns the *passage* that matched, which is what we want for question-answering.

The default strategy is `paragraph`: the SDK splits on blank lines, then groups paragraphs to roughly hit `chunkSize` tokens. `chunkOverlap` keeps a few tokens of shared context at every boundary so a sentence that straddles two chunks isn't lost.

`chunk: true` splits each document before embedding. The default `paragraph` strategy splits on blank lines, then groups paragraphs to roughly hit `chunkSize` tokens:

```ts
const result = await ragIngest({
  modelId,
  workspace: "tech",
  documents: samples,
  chunk: true,
  chunkOpts: {
    chunkSize: 200,
    chunkOverlap: 20,
  },
});
console.log(`Created ${result.processed.length} chunks from ${samples.length} documents`);
```

`processed` is now a list of chunks, not documents. Search against this workspace returns the specific passage that matched, not the whole document.

> Note: `chunkSize` is in tokens, not characters. A typical English word is roughly 1.3 tokens, so `chunkSize: 200` gives you chunks of about 150 words.