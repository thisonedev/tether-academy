---
title: "Run a pi05 action inference"
sourceExample: "packages/sdk/examples/vla-pi05.ts"
platforms: ['node']
tests:
  - id: loads-pi05
    description: 'Loads PI05_BASE_Q_AGGRESSIVE with modelType ggml-vla'
    pattern: "loadModel\\s*\\(\\s*\\{[^}]*modelType\\s*:\\s*[`\"']ggml-vla[`\"']"
  - id: reads-num-cameras
    description: 'Reads hparams.numCameras to size the images array'
    pattern: "hparams\\.numCameras"
  - id: empty-state
    description: 'Passes an empty Float32Array as the state buffer'
    pattern: "new\\s+Float32Array\\s*\\(\\s*0\\s*\\)"
  - id: calls-vla
    description: 'Calls vla({ modelId, images, state, tokens, mask, noise })'
    pattern: "vla\\s*\\(\\s*\\{[^}]*modelId[^}]*images[^}]*state"
hints:
  - "loadModel({ modelSrc: PI05_BASE_Q_AGGRESSIVE, modelType: 'ggml-vla', modelConfig: { backend: 'cpu' } }) pulls the π₀.₅ model from the registry."
  - "π₀.₅ expects hparams.numCameras frames, default 3. Build that many synthetic frames with vlaPreprocessImage."
  - "The state buffer is empty: `new Float32Array(0)`. The discrete-state input mode means the state is tokenised into the prompt and the buffer is ignored."
expectedOutput:
  - "▸ Loading π₀.₅ model..."
  - "▸ Model loaded: model-7f2a"
  - "▸ Backend: ggml-vla"
  - "▸ Running VLA inference..."
  - "▸ Got 50 action steps of dim 32."
  - "▸ Timing: vision=180ms prefill=1240ms ode=410ms total=1860ms"
questions:
  - id: why-numcameras-varies
    text: "What's the relationship between `numCameras` and the specific VLA model that's loaded?"
    answers:
      - text: "`numCameras` comes from this model's hparams, and π₀.₅'s value differs from SmolVLA's, so the frame count has to be read per model rather than assumed."
        correct: true
      - text: "Camera count is fixed by `imgWidth` and `imgHeight`; the model itself doesn't set it."
        correct: false
        feedback: "Think about what `hparams.numCameras` depends on."
      - text: "π₀.₅ always requires exactly 3 cameras, hardcoded in the SDK."
        correct: false
        feedback: "Check where the camera count comes from: a hardcoded constant, or `hparams.numCameras`."
  - id: how-to-run-pi05-inference
    text: "How do you run a π₀.₅ action inference once the model is loaded?"
    answers:
      - text: "Call `vla()` with the model id and the four prebuilt input buffers: images, state, tokens/mask, and noise."
        correct: true
      - text: "Call `vla()` with just the model id; the SDK builds the inputs internally."
        correct: false
        feedback: "Check what arguments the `vla()` call actually passes besides `modelId`."
      - text: "Call `loadModel()` a second time with the input buffers attached."
        correct: false
        feedback: "Check which function takes the input buffers: `loadModel()`, or `vla()`."
---

SmolVLA was the previous lesson's model. This lesson runs π₀.₅ from Physical Intelligence. The API surface is the same `vla()` call, but two hparams drive different input sizes.

`loadModel` pulls π₀.₅ into memory, and `vlaHparams` reads the input dimensions (`visionImageSize`, `numCameras`, `tokenizerMaxLength`, `chunkSize`, `maxActionDim`) for the next step:

```ts
const modelId = await loadModel({
  modelSrc: PI05_BASE_Q_AGGRESSIVE,
  modelType: "ggml-vla",
  modelConfig: { backend: "cpu" },
});

const { hparams } = await vlaHparams({ modelId });
const size = hparams.visionImageSize;
const numCameras = hparams.numCameras ?? 3;
```

The four input buffers π₀.₅ expects: `numCameras` synthetic camera frames via `vlaPreprocessImage`, a BOS-only `tokens` and `mask` (the model starts decoding from a single BOS token), an empty state buffer (the state is tokenised into the prompt in this model, so the buffer is ignored), and a `chunkSize × maxActionDim` zero-filled noise buffer the diffusion step denoises:

```ts
const dummyPixels = new Uint8Array(size * size * 3).fill(128);
const images = Array.from({ length: numCameras }, () =>
  vlaPreprocessImage(dummyPixels, size, size, { size }),
);

const tokens = new Int32Array(hparams.tokenizerMaxLength);
const mask = new Uint8Array(hparams.tokenizerMaxLength);
tokens[0] = 1;
mask[0] = 1;

const state = new Float32Array(0);
const noise = new Float32Array(hparams.chunkSize * hparams.maxActionDim);
```

`vla()` takes the model id and prebuilt inputs and returns the same four fields the SmolVLA lesson destructured. `stats` uses the same snake_case `*_ms` fields on both models, so the per-stage timings are interchangeable between lessons.

```ts
const { actions, actionDim, chunkSize, stats } = await vla({
  modelId,
  images,
  imgWidth: size,
  imgHeight: size,
  state,
  tokens,
  mask,
  noise,
});

console.log(`▸ Got ${chunkSize} action steps of dim ${actionDim}.`);
console.log(Array.from(actions.subarray(0, actionDim)));
if (stats) {
  console.log(
    `▸ Timing: vision=${stats.vision_ms?.toFixed(0)}ms ` +
      `prefill=${stats.prefill_total_ms?.toFixed(0)}ms ` +
      `ode=${stats.ode_ms?.toFixed(0)}ms ` +
      `total=${stats.total_ms?.toFixed(0)}ms`,
  );
}
```

The four `stats` fields are reported in processing order: vision encoder, language-model prefill, ODE solver, then wall-clock total. The optional chaining handles a stage that wasn't run. If a model short-circuits a phase, the field is `undefined` rather than missing from the object.

> Note: π₀.₅ is a larger model (~3.9 GB) than SmolVLA (~1.9 GB) and the inference time reflects that. On a desktop CPU, expect several seconds per call. The `backend: "cpu"` flag is the default and the only supported target in this SDK release.