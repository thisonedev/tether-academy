---
title: Translate text between languages
sourceExample: "packages/sdk/examples/translation/translation-bergamot.ts"
platforms: ['node']
tests:
  - id: sets-engine
    description: 'Sets engine: "Bergamot" in modelConfig'
    pattern: "engine\\s*:\\s*[`\"']Bergamot[`\"']"
  - id: calls-translate
    description: 'Calls translate({ modelId, text })'
    pattern: "translate\\s*\\(\\s*\\{[^}]*modelId[^}]*text"
hints:
  - "BERGAMOT_EN_FR is the English-to-French translation model constant in the SDK. Import it alongside loadModel and translate."
  - "Set modelConfig.engine to 'Bergamot' and pick the from and to languages, the model constant handles the file pair."
  - "translate({ modelId, text, modelType: 'nmtcpp-translation', stream: false }) returns the translated string via result.text."
expectedOutput:
  - "Translated text EN -> FR: This is a test of the Bergamot translation model. -> \"Ceci est un test du modèle de traduction Bergamot.\""
questions:
  - id: why-single-language-pair
    text: "What is a key characteristic of each Bergamot model that limits `BERGAMOT_EN_FR` to English-French only?"
    answers:
      - text: "`BERGAMOT_EN_FR` translates any pair; from and to in `modelConfig` pick the pair at inference time."
        correct: false
        feedback: "Check where from and to are set, and whether non-direct pairs translate without extra configuration."
      - text: "Each Bergamot model is trained for a single language pair; a different pair like EN to DE needs a different model constant."
        correct: true
      - text: "The 30 MB size limit is what restricts it to one language pair."
        correct: false
        feedback: "Think about why the model is small, versus what determines which languages it covers."
  - id: what-modeltype-does
    text: "What does `modelType: 'nmtcpp-translation'` do on the `translate()` call?"
    answers:
      - text: "It selects which language pair to translate between."
        correct: false
        feedback: "Check what from and to in `modelConfig` already handle, versus what `modelType` is for."
      - text: "It tells the SDK which addon to route the call through, distinguishing translation from transcription or text generation."
        correct: true
      - text: "It enables streaming output instead of a single `result.text` value."
        correct: false
        feedback: "Re-read what `stream: false` controls versus what `modelType` controls."
---

We're starting a new chapter on translation, and we're going to translate a string between two languages.

Bergamot is the on-device translation engine. Loading it looks like loading an LLM: pass a model constant, hand the engine a string, and get a translation back. Bergamot models are tiny (single-language pairs, around 30 MB), so they're cheap to keep in memory alongside other models.

Bergamot is a tiny neural translation model from Mozilla, single-language pairs around 30 MB each. The `engine: "Bergamot"` flag picks the right backend. The Bergamot load would look like the following:

```ts
const modelId = await loadModel({
  modelSrc: BERGAMOT_EN_FR,
  modelConfig: {
    engine: "Bergamot",
    from: "en",
    to: "fr",
    beamsize: 1,
  },
});
```

The inference step picks up where `loadModel` left off: hand the `modelId` to `translate({ text })`, await `result.text`, get the translated string. The two new flags are `modelType` (which addon to route the call to) and `stream: false` (sync, no duplex session):

```ts
const result = translate({
  modelId,
  text: "Hello, world.",
  modelType: "nmtcpp-translation",
  stream: false,
});
const translatedText = await result.text;
console.log(`EN -> FR: "${translatedText}"`);
```

The `modelType: "nmtcpp-translation"` flag tells the SDK which addon to route the call through. Without it, the SDK can't pick the right engine for translation vs transcription vs text generation.

> Note: each translation model is a single language pair. For EN to DE you'd load `BERGAMOT_EN_DE` instead. The SDK doesn't translate between non-direct pairs without explicit pivot configuration.