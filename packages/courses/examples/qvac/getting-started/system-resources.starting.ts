import { getSystemResources, type ResourceMetric } from "@qvac/sdk";

async function main() {
  // 1: get system resources with a live sample, then log cores and memory
}

main().catch(console.error);
