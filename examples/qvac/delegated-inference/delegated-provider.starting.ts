import { startQVACProvider } from "@qvac/sdk";

async function main() {
  // 1: read the seed and consumer key from argv, set the env if a seed is present

  // 2: call startQVACProvider with an optional firewall

  // 3: log the public key and the consumer command
}

main().catch(console.error);