import { loadModel } from "@qvac/sdk";

async function main() {
  // 1: load the multimodal model with its projection

  // 2: store the result in multimodalId and log it
}

main().catch(console.error);