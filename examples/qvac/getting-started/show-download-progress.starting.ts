import { loadModel, LLAMA_3_2_1B_INST_Q4_0 } from "@qvac/sdk";

async function main() {
  // 1: call loadModel() with modelSrc and an onProgress callback

  // 2: write a newline when p.percentage >= 100
}

main().catch(console.error);