import { loadModel, GTE_LARGE_FP16 } from "@qvac/sdk";

async function main() {
  // 1: call loadModel({ modelSrc: GTE_LARGE_FP16 })

  // 2: store the result in modelId and log it
}

main().catch(console.error);